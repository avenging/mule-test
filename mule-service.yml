apiVersion: v1
kind: Service
metadata:
  name: mule-test     
spec:
  selector:                  
    name: mule-test
  ports:
  - nodePort: 0
    port: 80       
    protocol: TCP
    targetPort: 8181
